name: push main

on:
  push:
    branches:
      - main

jobs:
  cicd:
    runs-on: ubuntu-latest
    steps:
      - name: HELLO WORLD
        run: echo "HELLO WORLD, THIS IS GITHUB ACTION!!!! ITS GOOD"
